<%= form_with(model: job_board, local: true) do |form| %>
  <% if job_board.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(job_board.errors.count, "error") %> prohibited this job_board from being saved:</h2>

      <ul>
      <% job_board.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :headline %>
    <%= form.text_field :headline %>
  </div>

  <div class="field">
    <%= form.label :content %>
    <%= form.text_area :content %>
  </div>

  <br>
  <div class="actions">
    <%= form.submit "submit new job", class:"waves-effect waves-light btn"  %>
  </div>
<% end %>
